# === Paste this ENTIRE file into PythonAnywhere → Web → WSGI configuration file ===
# Replace the default "Hello from Flask!" template completely.
#
# Also set (Web tab):
#   Source code:  /home/yuzi010129/calculator/imprint-calculator
#   Virtualenv:   /home/yuzi010129/calculator/imprint-calculator/venv
#   Static /static/ → /home/yuzi010129/calculator/imprint-calculator/static/
#
# On the server (once):
#   cd ~/calculator/imprint-calculator
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#   cp deploy/pythonanywhere.env.example .env
#   nano .env   # SECRET_KEY, ADMIN_PASSWORD, GOLD_XAU_PER_GRAM

import os
import sys

PROJECT_HOME = '/home/yuzi010129/calculator/imprint-calculator'

if PROJECT_HOME not in sys.path:
    sys.path.insert(0, PROJECT_HOME)

os.chdir(PROJECT_HOME)

env_file = os.path.join(PROJECT_HOME, '.env')
if os.path.isfile(env_file):
    from dotenv import load_dotenv
    load_dotenv(env_file, override=True)

from app import app as application
